const Loader = () => {
  return (
    <div className='w-full flex justify-center items-center h-[calc(100vh-64px)]'>
      <span className='loading loading-spinner loading-lg'></span>
    </div>
  );
};

export default Loader;
